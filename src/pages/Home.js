import React from 'react';
import {Ad} from "./Ad";
import {Container} from "react-bootstrap";

const Home = (props) => {
    return (
        <Container>
            <Ad/>
        </Container>
    )
};

export default Home;
